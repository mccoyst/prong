cxxflags = -std=c++11 -Wall -Werror
ldflags = -lSDL2

rule cx
 command = clang++ $cxxflags -c $in -o $out
 description = cx $in

rule ld
 command = clang++ $ldflags $in -o $out
 description = linking $out

build main.o: cx main.cxx

build prong: ld main.o
